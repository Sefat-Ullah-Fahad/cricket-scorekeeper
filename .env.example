# MongoDB Connection String (Fallback to persistent local storage when not provided)
MONGODB_URI=

# Authentication Secret
AUTH_SECRET=

# Base URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Google OAuth Credentials (Optional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
